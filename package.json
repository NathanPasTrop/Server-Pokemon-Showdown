{
  "name": "showdown-server",
  "version": "1.0.0",
  "engines": { "node": "20.x" },
  "scripts": {
    "postinstall": "cd pokemon-showdown && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {}
}
